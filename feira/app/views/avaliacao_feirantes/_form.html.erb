<%= form_with(model: avaliacao_feirante, html: { class: "form" }) do |form| %>
  <% if avaliacao_feirante.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(avaliacao_feirante.errors.count, "error") %> prohibited this avaliacao_feirante from being saved:</h2>

      <ul>
        <% avaliacao_feirante.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-field">
    <%= form.label :nota %>
    <%= form.number_field :nota, class: "input" %>
  </div>

  <div class="form-field">
    <%= form.label :comentario %>
    <%= form.text_area :comentario, class: "input" %>
  </div>

  <%= form.hidden_field :feirante_id %>

  <div class="button-double">
    <%= form.submit "Salvar", class: "button-confirm" %>
    <%= link_to 'Voltar', :back, class: "button-confirm" %>
  </div>
<% end %>
