<%= form_with(model: avaliacao_shopping) do |form| %>
  <% if avaliacao_shopping.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(avaliacao_shopping.errors.count, "error") %> prohibited this avaliacao_shopping from being saved:</h2>

      <ul>
        <% avaliacao_shopping.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nota, style: "display: block" %>
    <%= form.number_field :nota %>
  </div>

  <div>
    <%= form.label :comentario, style: "display: block" %>
    <%= form.text_area :comentario %>
  </div>

  <%= form.hidden_field :user_id %>
  <%= form.hidden_field :shopping_id %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
